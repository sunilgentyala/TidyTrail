source "https://rubygems.org"

# Pinned to a minor series so a compromised or breaking fastlane release
# can't silently change the signing/upload pipeline. Commit a Gemfile.lock
# (run `bundle lock` on a Mac) to pin every transitive gem as well.
gem "fastlane", "~> 2.240"
